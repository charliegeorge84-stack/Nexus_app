{
  "name": "process-change-management",
  "version": "1.0.0",
  "description": "Comprehensive Process Change Management Platform with admin controls, integration capabilities, and robust notification systems",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "start": "cd server && npm start"
  },
  "keywords": [
    "process-management",
    "change-management",
    "workflow",
    "admin-console",
    "notifications"
  ],
  "author": "Process Change Management Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
